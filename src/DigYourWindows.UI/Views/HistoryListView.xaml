<UserControl
    x:Class="DigYourWindows.UI.Views.HistoryListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="800">

    <Grid>
        <StackPanel Orientation="Vertical" Margin="16">
            <TextBlock Text="诊断历史记录" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />

            <!-- Empty State -->
            <TextBlock
                x:Name="EmptyStateText"
                Text="无可用的诊断历史记录。"
                Visibility="{Binding IsHistoryEmpty, Converter={StaticResource BoolToVisibilityConverter}}"
                Foreground="Gray"
                Margin="0,16" />

            <!-- Loading State -->
            <TextBlock
                Text="正在加载历史记录..."
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                Foreground="Gray"
                Margin="0,16" />

            <!-- History List -->
            <ListBox
                x:Name="HistoryListBox"
                ItemsSource="{Binding HistoryEntries}"
                SelectedItem="{Binding SelectedEntry}"
                Visibility="{Binding IsHistoryEmpty, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                Margin="0,16">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="8" Background="White" Margin="0,4" CornerRadius="4">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding ComputerName, StringFormat='计算机: {0}'}" FontWeight="Bold" />
                                <TextBlock Text="{Binding CollectedAtUtc, StringFormat='收集时间: {0:G}'}" Foreground="Gray" FontSize="12" />
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding HealthGrade, StringFormat='等级: {0}'}" Foreground="DarkGreen" FontWeight="Bold" Margin="0,0,16,0" />
                                    <TextBlock Text="{Binding SystemHealthScore, StringFormat='分数: {0:F1}'}" Foreground="DarkBlue" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>

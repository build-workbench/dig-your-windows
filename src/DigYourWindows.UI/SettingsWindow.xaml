<ui:FluentWindow x:Class="DigYourWindows.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="设置"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <StackPanel Margin="28,24">
        <TextBlock Text="外观" FontSize="16" FontWeight="SemiBold"/>

        <TextBlock Text="界面字体" Margin="0,16,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        <ComboBox x:Name="FontComboBox"
                  DisplayMemberPath="DisplayName"
                  SelectedValuePath="FontName"/>

        <TextBlock Text="界面缩放" Margin="0,14,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        <ComboBox x:Name="ScaleComboBox"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <ui:Button Content="确定" Appearance="Primary" Click="OnOkClicked" MinWidth="88"/>
            <ui:Button Content="取消" Margin="10,0,0,0" Click="OnCancelClicked" MinWidth="88"/>
        </StackPanel>
    </StackPanel>
</ui:FluentWindow>
